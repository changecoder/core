#!/usr/bin/env node

const program = require('commander')
const Site = require('../site/webpack-dev-server')

program
  .option('-c, --config <path>', 'set config path. defaults to ./webpack.config.js')
  .parse(process.argv)

Site.start(program)